apiVersion: v1
kind: ConfigMap
metadata:
  name: vcloud-ccm-configmap
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "labels.common" . | nindent 4 }}
data:
{{- with .Values.cpiConfigmap }}
  vcloud-ccm-config.yaml: |+
    vcd:
      host: {{ .site }}
      org: {{ .org }}
      vdc: {{ .ovdc }}
      vAppName: {{ .vAppName }}
      network: {{ .ovdcNetwork }}
      vipSubnet: {{ .vipSubnet }}
    loadbalancer:
      oneArm:
        startIP: {{ .oneArm.startIP }}
        endIP: {{ .oneArm.endIP }}
      ports:
        http: 80
        https: 443
      certAlias: {{ .clusterid }}-cert
    clusterid: {{ .clusterid }} 
immutable: {{ .immutable }}
{{- end }}